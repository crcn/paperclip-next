// Test file with lint violations for viewport units and negative spacing

public token primaryColor #3366FF

// Style with viewport units (should trigger lint error)
style fullHeight {
  height: 100vh
  width: 50vw
}

// Style with negative spacing (should trigger lint error)
style negativeMargins {
  margin-top: -10px
  padding: -5px
}

// Component with multiple violations
public component TestButton {
  render button {
    style {
      background: blue
      height: 80vh
      margin: -15px
      padding: 10px
    }
    text "Test"
  }
}

// Component with valid styles (should pass)
public component GoodButton {
  render button {
    style {
      background: green
      height: 50px
      margin: 10px
      padding: 8px
    }
    text "Good"
  }
}

// Style with mixed issues
style mixedIssues {
  width: 75vw
  margin-left: -20px
  padding-top: -8px
  color: red
}
