// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: patches.proto
/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { CssRule, VDocument, VNode } from "./vdom";
export const protobufPackage = "paperclip.patches";
function createBaseVDocPatch() {
    return {
        initialize: undefined,
        createNode: undefined,
        removeNode: undefined,
        replaceNode: undefined,
        updateAttributes: undefined,
        updateStyles: undefined,
        updateText: undefined,
        addStyleRule: undefined,
        removeStyleRule: undefined,
        moveChild: undefined,
    };
}
export const VDocPatch = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.initialize !== undefined) {
            InitializePatch.encode(message.initialize, writer.uint32(10).fork()).ldelim();
        }
        if (message.createNode !== undefined) {
            CreateNodePatch.encode(message.createNode, writer.uint32(18).fork()).ldelim();
        }
        if (message.removeNode !== undefined) {
            RemoveNodePatch.encode(message.removeNode, writer.uint32(26).fork()).ldelim();
        }
        if (message.replaceNode !== undefined) {
            ReplaceNodePatch.encode(message.replaceNode, writer.uint32(34).fork()).ldelim();
        }
        if (message.updateAttributes !== undefined) {
            UpdateAttributesPatch.encode(message.updateAttributes, writer.uint32(42).fork()).ldelim();
        }
        if (message.updateStyles !== undefined) {
            UpdateStylesPatch.encode(message.updateStyles, writer.uint32(50).fork()).ldelim();
        }
        if (message.updateText !== undefined) {
            UpdateTextPatch.encode(message.updateText, writer.uint32(58).fork()).ldelim();
        }
        if (message.addStyleRule !== undefined) {
            AddStyleRulePatch.encode(message.addStyleRule, writer.uint32(66).fork()).ldelim();
        }
        if (message.removeStyleRule !== undefined) {
            RemoveStyleRulePatch.encode(message.removeStyleRule, writer.uint32(74).fork()).ldelim();
        }
        if (message.moveChild !== undefined) {
            MoveChildPatch.encode(message.moveChild, writer.uint32(82).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVDocPatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.initialize = InitializePatch.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.createNode = CreateNodePatch.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.removeNode = RemoveNodePatch.decode(reader, reader.uint32());
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.replaceNode = ReplaceNodePatch.decode(reader, reader.uint32());
                    continue;
                case 5:
                    if (tag !== 42) {
                        break;
                    }
                    message.updateAttributes = UpdateAttributesPatch.decode(reader, reader.uint32());
                    continue;
                case 6:
                    if (tag !== 50) {
                        break;
                    }
                    message.updateStyles = UpdateStylesPatch.decode(reader, reader.uint32());
                    continue;
                case 7:
                    if (tag !== 58) {
                        break;
                    }
                    message.updateText = UpdateTextPatch.decode(reader, reader.uint32());
                    continue;
                case 8:
                    if (tag !== 66) {
                        break;
                    }
                    message.addStyleRule = AddStyleRulePatch.decode(reader, reader.uint32());
                    continue;
                case 9:
                    if (tag !== 74) {
                        break;
                    }
                    message.removeStyleRule = RemoveStyleRulePatch.decode(reader, reader.uint32());
                    continue;
                case 10:
                    if (tag !== 82) {
                        break;
                    }
                    message.moveChild = MoveChildPatch.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            initialize: isSet(object.initialize) ? InitializePatch.fromJSON(object.initialize) : undefined,
            createNode: isSet(object.createNode) ? CreateNodePatch.fromJSON(object.createNode) : undefined,
            removeNode: isSet(object.removeNode) ? RemoveNodePatch.fromJSON(object.removeNode) : undefined,
            replaceNode: isSet(object.replaceNode) ? ReplaceNodePatch.fromJSON(object.replaceNode) : undefined,
            updateAttributes: isSet(object.updateAttributes)
                ? UpdateAttributesPatch.fromJSON(object.updateAttributes)
                : undefined,
            updateStyles: isSet(object.updateStyles) ? UpdateStylesPatch.fromJSON(object.updateStyles) : undefined,
            updateText: isSet(object.updateText) ? UpdateTextPatch.fromJSON(object.updateText) : undefined,
            addStyleRule: isSet(object.addStyleRule) ? AddStyleRulePatch.fromJSON(object.addStyleRule) : undefined,
            removeStyleRule: isSet(object.removeStyleRule)
                ? RemoveStyleRulePatch.fromJSON(object.removeStyleRule)
                : undefined,
            moveChild: isSet(object.moveChild) ? MoveChildPatch.fromJSON(object.moveChild) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.initialize !== undefined) {
            obj.initialize = InitializePatch.toJSON(message.initialize);
        }
        if (message.createNode !== undefined) {
            obj.createNode = CreateNodePatch.toJSON(message.createNode);
        }
        if (message.removeNode !== undefined) {
            obj.removeNode = RemoveNodePatch.toJSON(message.removeNode);
        }
        if (message.replaceNode !== undefined) {
            obj.replaceNode = ReplaceNodePatch.toJSON(message.replaceNode);
        }
        if (message.updateAttributes !== undefined) {
            obj.updateAttributes = UpdateAttributesPatch.toJSON(message.updateAttributes);
        }
        if (message.updateStyles !== undefined) {
            obj.updateStyles = UpdateStylesPatch.toJSON(message.updateStyles);
        }
        if (message.updateText !== undefined) {
            obj.updateText = UpdateTextPatch.toJSON(message.updateText);
        }
        if (message.addStyleRule !== undefined) {
            obj.addStyleRule = AddStyleRulePatch.toJSON(message.addStyleRule);
        }
        if (message.removeStyleRule !== undefined) {
            obj.removeStyleRule = RemoveStyleRulePatch.toJSON(message.removeStyleRule);
        }
        if (message.moveChild !== undefined) {
            obj.moveChild = MoveChildPatch.toJSON(message.moveChild);
        }
        return obj;
    },
    create(base) {
        return VDocPatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseVDocPatch();
        message.initialize = (object.initialize !== undefined && object.initialize !== null)
            ? InitializePatch.fromPartial(object.initialize)
            : undefined;
        message.createNode = (object.createNode !== undefined && object.createNode !== null)
            ? CreateNodePatch.fromPartial(object.createNode)
            : undefined;
        message.removeNode = (object.removeNode !== undefined && object.removeNode !== null)
            ? RemoveNodePatch.fromPartial(object.removeNode)
            : undefined;
        message.replaceNode = (object.replaceNode !== undefined && object.replaceNode !== null)
            ? ReplaceNodePatch.fromPartial(object.replaceNode)
            : undefined;
        message.updateAttributes = (object.updateAttributes !== undefined && object.updateAttributes !== null)
            ? UpdateAttributesPatch.fromPartial(object.updateAttributes)
            : undefined;
        message.updateStyles = (object.updateStyles !== undefined && object.updateStyles !== null)
            ? UpdateStylesPatch.fromPartial(object.updateStyles)
            : undefined;
        message.updateText = (object.updateText !== undefined && object.updateText !== null)
            ? UpdateTextPatch.fromPartial(object.updateText)
            : undefined;
        message.addStyleRule = (object.addStyleRule !== undefined && object.addStyleRule !== null)
            ? AddStyleRulePatch.fromPartial(object.addStyleRule)
            : undefined;
        message.removeStyleRule = (object.removeStyleRule !== undefined && object.removeStyleRule !== null)
            ? RemoveStyleRulePatch.fromPartial(object.removeStyleRule)
            : undefined;
        message.moveChild = (object.moveChild !== undefined && object.moveChild !== null)
            ? MoveChildPatch.fromPartial(object.moveChild)
            : undefined;
        return message;
    },
};
function createBasePatchPath() {
    return { positional: undefined, semantic: undefined };
}
export const PatchPath = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.positional !== undefined) {
            PositionalPath.encode(message.positional, writer.uint32(10).fork()).ldelim();
        }
        if (message.semantic !== undefined) {
            writer.uint32(18).string(message.semantic);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePatchPath();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.positional = PositionalPath.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.semantic = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            positional: isSet(object.positional) ? PositionalPath.fromJSON(object.positional) : undefined,
            semantic: isSet(object.semantic) ? globalThis.String(object.semantic) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.positional !== undefined) {
            obj.positional = PositionalPath.toJSON(message.positional);
        }
        if (message.semantic !== undefined) {
            obj.semantic = message.semantic;
        }
        return obj;
    },
    create(base) {
        return PatchPath.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePatchPath();
        message.positional = (object.positional !== undefined && object.positional !== null)
            ? PositionalPath.fromPartial(object.positional)
            : undefined;
        message.semantic = object.semantic ?? undefined;
        return message;
    },
};
function createBasePositionalPath() {
    return { indices: [] };
}
export const PositionalPath = {
    encode(message, writer = _m0.Writer.create()) {
        writer.uint32(10).fork();
        for (const v of message.indices) {
            writer.uint32(v);
        }
        writer.ldelim();
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePositionalPath();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag === 8) {
                        message.indices.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.indices.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            indices: globalThis.Array.isArray(object?.indices) ? object.indices.map((e) => globalThis.Number(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.indices?.length) {
            obj.indices = message.indices.map((e) => Math.round(e));
        }
        return obj;
    },
    create(base) {
        return PositionalPath.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePositionalPath();
        message.indices = object.indices?.map((e) => e) || [];
        return message;
    },
};
function createBaseInitializePatch() {
    return { vdom: undefined };
}
export const InitializePatch = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.vdom !== undefined) {
            VDocument.encode(message.vdom, writer.uint32(10).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseInitializePatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.vdom = VDocument.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { vdom: isSet(object.vdom) ? VDocument.fromJSON(object.vdom) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.vdom !== undefined) {
            obj.vdom = VDocument.toJSON(message.vdom);
        }
        return obj;
    },
    create(base) {
        return InitializePatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseInitializePatch();
        message.vdom = (object.vdom !== undefined && object.vdom !== null) ? VDocument.fromPartial(object.vdom) : undefined;
        return message;
    },
};
function createBaseCreateNodePatch() {
    return { path: [], node: undefined, index: 0 };
}
export const CreateNodePatch = {
    encode(message, writer = _m0.Writer.create()) {
        writer.uint32(10).fork();
        for (const v of message.path) {
            writer.uint32(v);
        }
        writer.ldelim();
        if (message.node !== undefined) {
            VNode.encode(message.node, writer.uint32(18).fork()).ldelim();
        }
        if (message.index !== 0) {
            writer.uint32(24).uint32(message.index);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCreateNodePatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag === 8) {
                        message.path.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.path.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.node = VNode.decode(reader, reader.uint32());
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.index = reader.uint32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            path: globalThis.Array.isArray(object?.path) ? object.path.map((e) => globalThis.Number(e)) : [],
            node: isSet(object.node) ? VNode.fromJSON(object.node) : undefined,
            index: isSet(object.index) ? globalThis.Number(object.index) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.path?.length) {
            obj.path = message.path.map((e) => Math.round(e));
        }
        if (message.node !== undefined) {
            obj.node = VNode.toJSON(message.node);
        }
        if (message.index !== 0) {
            obj.index = Math.round(message.index);
        }
        return obj;
    },
    create(base) {
        return CreateNodePatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCreateNodePatch();
        message.path = object.path?.map((e) => e) || [];
        message.node = (object.node !== undefined && object.node !== null) ? VNode.fromPartial(object.node) : undefined;
        message.index = object.index ?? 0;
        return message;
    },
};
function createBaseRemoveNodePatch() {
    return { path: [] };
}
export const RemoveNodePatch = {
    encode(message, writer = _m0.Writer.create()) {
        writer.uint32(10).fork();
        for (const v of message.path) {
            writer.uint32(v);
        }
        writer.ldelim();
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRemoveNodePatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag === 8) {
                        message.path.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.path.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { path: globalThis.Array.isArray(object?.path) ? object.path.map((e) => globalThis.Number(e)) : [] };
    },
    toJSON(message) {
        const obj = {};
        if (message.path?.length) {
            obj.path = message.path.map((e) => Math.round(e));
        }
        return obj;
    },
    create(base) {
        return RemoveNodePatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRemoveNodePatch();
        message.path = object.path?.map((e) => e) || [];
        return message;
    },
};
function createBaseReplaceNodePatch() {
    return { path: [], newNode: undefined };
}
export const ReplaceNodePatch = {
    encode(message, writer = _m0.Writer.create()) {
        writer.uint32(10).fork();
        for (const v of message.path) {
            writer.uint32(v);
        }
        writer.ldelim();
        if (message.newNode !== undefined) {
            VNode.encode(message.newNode, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseReplaceNodePatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag === 8) {
                        message.path.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.path.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.newNode = VNode.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            path: globalThis.Array.isArray(object?.path) ? object.path.map((e) => globalThis.Number(e)) : [],
            newNode: isSet(object.newNode) ? VNode.fromJSON(object.newNode) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.path?.length) {
            obj.path = message.path.map((e) => Math.round(e));
        }
        if (message.newNode !== undefined) {
            obj.newNode = VNode.toJSON(message.newNode);
        }
        return obj;
    },
    create(base) {
        return ReplaceNodePatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseReplaceNodePatch();
        message.path = object.path?.map((e) => e) || [];
        message.newNode = (object.newNode !== undefined && object.newNode !== null)
            ? VNode.fromPartial(object.newNode)
            : undefined;
        return message;
    },
};
function createBaseUpdateAttributesPatch() {
    return { path: [], attributes: {} };
}
export const UpdateAttributesPatch = {
    encode(message, writer = _m0.Writer.create()) {
        writer.uint32(10).fork();
        for (const v of message.path) {
            writer.uint32(v);
        }
        writer.ldelim();
        Object.entries(message.attributes).forEach(([key, value]) => {
            UpdateAttributesPatch_AttributesEntry.encode({ key: key, value }, writer.uint32(18).fork()).ldelim();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateAttributesPatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag === 8) {
                        message.path.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.path.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    const entry2 = UpdateAttributesPatch_AttributesEntry.decode(reader, reader.uint32());
                    if (entry2.value !== undefined) {
                        message.attributes[entry2.key] = entry2.value;
                    }
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            path: globalThis.Array.isArray(object?.path) ? object.path.map((e) => globalThis.Number(e)) : [],
            attributes: isObject(object.attributes)
                ? Object.entries(object.attributes).reduce((acc, [key, value]) => {
                    acc[key] = String(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.path?.length) {
            obj.path = message.path.map((e) => Math.round(e));
        }
        if (message.attributes) {
            const entries = Object.entries(message.attributes);
            if (entries.length > 0) {
                obj.attributes = {};
                entries.forEach(([k, v]) => {
                    obj.attributes[k] = v;
                });
            }
        }
        return obj;
    },
    create(base) {
        return UpdateAttributesPatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateAttributesPatch();
        message.path = object.path?.map((e) => e) || [];
        message.attributes = Object.entries(object.attributes ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseUpdateAttributesPatch_AttributesEntry() {
    return { key: "", value: "" };
}
export const UpdateAttributesPatch_AttributesEntry = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== "") {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateAttributesPatch_AttributesEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.value = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.String(object.value) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== "") {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return UpdateAttributesPatch_AttributesEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateAttributesPatch_AttributesEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? "";
        return message;
    },
};
function createBaseUpdateStylesPatch() {
    return { path: [], styles: {} };
}
export const UpdateStylesPatch = {
    encode(message, writer = _m0.Writer.create()) {
        writer.uint32(10).fork();
        for (const v of message.path) {
            writer.uint32(v);
        }
        writer.ldelim();
        Object.entries(message.styles).forEach(([key, value]) => {
            UpdateStylesPatch_StylesEntry.encode({ key: key, value }, writer.uint32(18).fork()).ldelim();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateStylesPatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag === 8) {
                        message.path.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.path.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    const entry2 = UpdateStylesPatch_StylesEntry.decode(reader, reader.uint32());
                    if (entry2.value !== undefined) {
                        message.styles[entry2.key] = entry2.value;
                    }
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            path: globalThis.Array.isArray(object?.path) ? object.path.map((e) => globalThis.Number(e)) : [],
            styles: isObject(object.styles)
                ? Object.entries(object.styles).reduce((acc, [key, value]) => {
                    acc[key] = String(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.path?.length) {
            obj.path = message.path.map((e) => Math.round(e));
        }
        if (message.styles) {
            const entries = Object.entries(message.styles);
            if (entries.length > 0) {
                obj.styles = {};
                entries.forEach(([k, v]) => {
                    obj.styles[k] = v;
                });
            }
        }
        return obj;
    },
    create(base) {
        return UpdateStylesPatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateStylesPatch();
        message.path = object.path?.map((e) => e) || [];
        message.styles = Object.entries(object.styles ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseUpdateStylesPatch_StylesEntry() {
    return { key: "", value: "" };
}
export const UpdateStylesPatch_StylesEntry = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== "") {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateStylesPatch_StylesEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.value = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.String(object.value) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== "") {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return UpdateStylesPatch_StylesEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateStylesPatch_StylesEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? "";
        return message;
    },
};
function createBaseUpdateTextPatch() {
    return { path: [], content: "" };
}
export const UpdateTextPatch = {
    encode(message, writer = _m0.Writer.create()) {
        writer.uint32(10).fork();
        for (const v of message.path) {
            writer.uint32(v);
        }
        writer.ldelim();
        if (message.content !== "") {
            writer.uint32(18).string(message.content);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateTextPatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag === 8) {
                        message.path.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.path.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.content = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            path: globalThis.Array.isArray(object?.path) ? object.path.map((e) => globalThis.Number(e)) : [],
            content: isSet(object.content) ? globalThis.String(object.content) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.path?.length) {
            obj.path = message.path.map((e) => Math.round(e));
        }
        if (message.content !== "") {
            obj.content = message.content;
        }
        return obj;
    },
    create(base) {
        return UpdateTextPatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateTextPatch();
        message.path = object.path?.map((e) => e) || [];
        message.content = object.content ?? "";
        return message;
    },
};
function createBaseAddStyleRulePatch() {
    return { rule: undefined };
}
export const AddStyleRulePatch = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.rule !== undefined) {
            CssRule.encode(message.rule, writer.uint32(10).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAddStyleRulePatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.rule = CssRule.decode(reader, reader.uint32());
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { rule: isSet(object.rule) ? CssRule.fromJSON(object.rule) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.rule !== undefined) {
            obj.rule = CssRule.toJSON(message.rule);
        }
        return obj;
    },
    create(base) {
        return AddStyleRulePatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseAddStyleRulePatch();
        message.rule = (object.rule !== undefined && object.rule !== null) ? CssRule.fromPartial(object.rule) : undefined;
        return message;
    },
};
function createBaseRemoveStyleRulePatch() {
    return { index: 0 };
}
export const RemoveStyleRulePatch = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.index !== 0) {
            writer.uint32(8).uint32(message.index);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRemoveStyleRulePatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.index = reader.uint32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { index: isSet(object.index) ? globalThis.Number(object.index) : 0 };
    },
    toJSON(message) {
        const obj = {};
        if (message.index !== 0) {
            obj.index = Math.round(message.index);
        }
        return obj;
    },
    create(base) {
        return RemoveStyleRulePatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRemoveStyleRulePatch();
        message.index = object.index ?? 0;
        return message;
    },
};
function createBaseMoveChildPatch() {
    return { parent: undefined, childSemanticId: "", fromIndex: 0, toIndex: 0 };
}
export const MoveChildPatch = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.parent !== undefined) {
            PatchPath.encode(message.parent, writer.uint32(10).fork()).ldelim();
        }
        if (message.childSemanticId !== "") {
            writer.uint32(18).string(message.childSemanticId);
        }
        if (message.fromIndex !== 0) {
            writer.uint32(24).uint32(message.fromIndex);
        }
        if (message.toIndex !== 0) {
            writer.uint32(32).uint32(message.toIndex);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMoveChildPatch();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.parent = PatchPath.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.childSemanticId = reader.string();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.fromIndex = reader.uint32();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.toIndex = reader.uint32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            parent: isSet(object.parent) ? PatchPath.fromJSON(object.parent) : undefined,
            childSemanticId: isSet(object.childSemanticId) ? globalThis.String(object.childSemanticId) : "",
            fromIndex: isSet(object.fromIndex) ? globalThis.Number(object.fromIndex) : 0,
            toIndex: isSet(object.toIndex) ? globalThis.Number(object.toIndex) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.parent !== undefined) {
            obj.parent = PatchPath.toJSON(message.parent);
        }
        if (message.childSemanticId !== "") {
            obj.childSemanticId = message.childSemanticId;
        }
        if (message.fromIndex !== 0) {
            obj.fromIndex = Math.round(message.fromIndex);
        }
        if (message.toIndex !== 0) {
            obj.toIndex = Math.round(message.toIndex);
        }
        return obj;
    },
    create(base) {
        return MoveChildPatch.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseMoveChildPatch();
        message.parent = (object.parent !== undefined && object.parent !== null)
            ? PatchPath.fromPartial(object.parent)
            : undefined;
        message.childSemanticId = object.childSemanticId ?? "";
        message.fromIndex = object.fromIndex ?? 0;
        message.toIndex = object.toIndex ?? 0;
        return message;
    },
};
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=patches.js.map